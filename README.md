# 🎃 Лабораторная работа №4
## Алгоритмы восстановления плотности вероятности • Смеси распределений
### Pumpkin Seeds ML Analysis

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)]()
[![Libraries](https://img.shields.io/badge/Scikit--Learn-ML-blue.svg)]()
[![Status](https://img.shields.io/badge/Status-Completed-brightgreen.svg)]()
[![Author](https://img.shields.io/badge/Author-Альберт-orange.svg)]()

---

## 📦 Описание проекта

Лабораторная работа посвящена методам восстановления плотности вероятности:

- параметрическим,
- непараметрическим (KDE),
- смесям распределений (GMM),
- и сравнению их в задаче бинарной классификации.

Работа выполнена на **Pumpkin Seeds Dataset**
(характеристики семечек тыквы для задач классификации).

Все результаты визуализируются и сохраняются в каталог:
```bash
./figures/
```


---

## 📁 Структура проекта

lab4/
│── main.py # Основной скрипт лабораторной
│── Pumpkin_Seeds_Dataset.xlsx
│── README.md
└── figures/
├── lab4_task1_f1.png
├── lab4_task1_f2.png
├── lab4_task2_f1.png
├── lab4_task2_f2.png
├── lab4_task3_f1_gmm.png
├── lab4_task3_f2_gmm.png
└── ...


---

## 🔹 1. Параметрический подход (Normal Distribution)

Выбран самый частый класс по целевой переменной.
Для признаков:

- **Area**
- **Perimeter**

были вычислены:

- среднее μ
- стандартное отклонение σ

и построены PDF нормального распределения поверх гистограмм.

### 📊 Графики:

| Признак | Файл |
|--------|-------|
| Area | `lab4_task1_f1.png` |
| Perimeter | `lab4_task1_f2.png` |

---

## 🔹 2. Непараметрическое восстановление плотности (KDE)

Использована `KernelDensity` (Gaussian kernel).

Подобрана ширина окна (**bandwidth**):

- визуально,
- по максимальному log-likelihood (ML) на тестовой выборке.

На графиках сравниваются обе аппроксимации.

### 📊 Графики:

| Признак | Файл |
|--------|-------|
| Area | `lab4_task2_f1.png` |
| Perimeter | `lab4_task2_f2.png` |

---

## 🔹 3. Смеси распределений (Gaussian Mixture Models)

Использована модель:

- `GaussianMixture(n_components=2)`

Для каждого признака построены:

- суммарная плотность смеси,
- плотности отдельных компонент.

В консоль дополнительно выводятся параметры смеси:

- веса `wᵢ`
- средние `μᵢ`
- стандартные отклонения `σᵢ`
- вклад компоненты в итоговую плотность

### 📊 Графики:

| Признак | Файл |
|--------|-------|
| Area | `lab4_task3_f1_gmm.png` |
| Perimeter | `lab4_task3_f2_gmm.png` |

---

## 🔹 4. Наивный байесовский классификатор

Построены три версии вероятностной модели:

- **Параметрическая (Normal PDF)**
- **KDE-модель**
- **Gaussian Mixture Model (GMM)**
- **GaussianNB (sklearn)**

Сравнение AUC:

| Метод | AUC |
|-------|------|
| Parametric | 0.5845 |
| KDE | 0.5107 |
| GMM | 0.6438 |
| GaussianNB | 0.5845 |

---

## 🧪 Установка и запуск

### 1. Установка зависимостей

```bash
pip install numpy pandas matplotlib scikit-learn scipy openpyxl
```

# 2. Запуск лабораторной

```bash
python main.py
```

### После выполнения все результаты появятся в:
```bash
./figures/
```

## 🎯 Основные выводы

- **Gaussian Mixture Model (GMM)** показал наибольший AUC
  → это говорит о **многомодальной структуре данных** и том, что смесь гауссов лучше описывает распределения признаков.

- **KDE (ядерная оценка плотности)** сильно зависит от параметра `bandwidth`:
  - маленький → **переобучение**, плотность становится слишком рваной
  - большой → **потеря структуры**, чрезмерное сглаживание

- **Parametric Normal** и **GaussianNB** дают практически одинаковые результаты
  → это подтверждает **корректность ручной реализации нормальной плотности**.

- Нормальные распределения дают адекватное приближение,
  **но смесь Гауссов (GMM) моделирует распределение качественнее**, что отражается в лучшем AUC.

---

## 📨 Контакты

**Автор:** Альберт
**GitHub:** https://github.com/alnubvar
